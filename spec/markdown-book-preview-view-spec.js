'use babel';

import MarkdownBookPreviewView from '../lib/markdown-book-preview-view';

describe('MarkdownBookPreviewView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
